/**
 *  Draggable v0.4
 *
 *  This Drag&Drop library can be used both touch and click devices.
 *
 *  - DEMO -
 *   http://the-next.tv/draggable/
 *
 *  - Simple Usage -
 *  1. Import Javascript files extension.js -> droppable.js -> draggable.js
 *
 *  2. Initiate Draggable Objests with element ID or element itself.
 *     callback function is called if draggable object dropped at droppable object.
 *     - var draggable = new Draggable('draggable1', function(id) {
 *	         // alert(id);
 *       });
 *
 *  3. Initiate Droppable Objects with element ID or element itself.
 *     callback function is called if draggable object dropped at.
 *     - var dp1 = droppable.add('dropable1', function(id) {
 *	         document.getElementById(id).style.display = 'none';
 *       });
 *
 */